pipeline {
  agent any
  options { 
    buildDiscarder(logRotator(numToKeepStr: '2'))
  }
      stages{
        stage('Say Hello'){
          steps{
                echo 'Hello'
                sh 'java -version'
              }
           }
         }
  
}
